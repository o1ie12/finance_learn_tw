// TODO(placeholder): swap in the real contact address before /about, /privacy,
// or /terms ship — see the build notes. Not wired to any inbox yet. Shared
// here (rather than duplicated per page) so there's exactly one place to
// update once a real address exists.
export const CONTACT_EMAIL = "hello@qidian.tw";
